# Local development only (Vite loads this file solely in `vite dev`, never in a
# production build). Production must set VITE_BACKEND_URL in the host (Vercel).
VITE_BACKEND_URL=http://localhost:8000
REACT_APP_BACKEND_URL=http://localhost:5174

WDS_SOCKET_PORT=443
ENABLE_HEALTH_CHECK=false
