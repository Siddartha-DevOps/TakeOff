# Prisma Postgres connection string — written to .env by `npx prisma postgres link`.
# .env is gitignored; never commit the real value.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
