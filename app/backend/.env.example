# Model weights bucket (infra/modal_gpu.py trains into this; the backend
# reads trained weights back out of it). Separate from S3_BUCKET
# (storage.py — user-uploaded drawings), a different bucket for a
# different concern.
AI_MODELS_BUCKET=takeoff-ai-models
