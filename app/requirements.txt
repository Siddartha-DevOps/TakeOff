# TakeOff.ai — AI/ML Requirements
# Install: pip install -r requirements.txt --break-system-packages
#
# These are the heavy AI/ML dependencies (torch, ultralytics, OCR, CLIP).
# The lightweight API server deps live in backend/requirements.txt.
# The detection engine degrades gracefully if this stack is absent or no
# trained weights exist (see detection_engine.BlueprintDetector), so the
# API can run on backend/requirements.txt alone during development.

# Core deep learning
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# YOLO (object detection + instance segmentation)
ultralytics==8.2.0

# OCR (scale detection + text search)
paddlepaddle==2.6.1
paddleocr==2.7.3

# CLIP (image search embeddings)
ftfy==6.1.3
regex
git+https://github.com/openai/CLIP.git

# Image processing
opencv-python-headless==4.9.0.80
Pillow==10.3.0
numpy==1.26.4
scipy==1.13.0
scikit-image==0.23.2

# PDF processing (convert PDF pages to images for AI)
pdf2image==1.17.0
PyMuPDF==1.24.3

# Vector embeddings storage
pgvector==0.2.5
sentence-transformers==3.0.1

# Async task queue (AI jobs must NOT block FastAPI)
celery==5.4.0
redis==5.0.6
flower==2.0.1  # Celery monitoring

# AWS integration
boto3==1.34.131
sagemaker==2.224.1

# Data / training utilities
albumentations==1.4.8     # augmentation
pycocotools==2.0.7        # COCO format annotation parsing
supervision==0.21.0       # detection visualization + metrics
tqdm==4.66.4
matplotlib==3.9.0
seaborn==0.13.2

# API
fastapi==0.110.1
httpx==0.27.0

# Utilities
python-dotenv==1.0.1
pydantic==2.7.1
loguru==0.7.2